// @fail
// @error "无可用结果"

// `::` 编译期绑定但条件依赖运行时变量 b：应干净报级联错误，
// 不能重复上报、不能误报"循环依赖"、不能崩
main :: () -> i32 {
    b := 2;
    f :: if b == 2 then (a: i32) -> i32 { return a; } else (a: i32) -> i32 { return a + 1; }
    return f(1);
}
