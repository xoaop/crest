// @fail
// @error "字段访问不支持的类型的值"

// 对指针值取字段应干净报错、不崩 type_val()。
// 指针不能作编译期常量（check_const_type 已拦），故用运行时变量触达 analyze_FieldAccess。

main :: () -> i32 {
    p := "hello".data;
    x := p.count;
    return 0;
}
