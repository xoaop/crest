// @pass
// @run

// 取地址得到的函数值走运行期调用: ResolveGenericCallee 必须原样透传被调对象
twice :: (n: i32) -> i32 {

    return n * 2i32;
}

main :: () -> i32 {

    f := &twice;

    r := f(21i32);
    if r != 42i32 { return 1; }

    return 0;
}
